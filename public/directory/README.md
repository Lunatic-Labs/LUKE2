# Directory Photos

Place staff portrait images in this folder. Each filename should match the `image` value for that person in `features/scenes/directory/DirectoryScene.tsx` (for example, `Algood_Amy_web3.jpg`).

The directory scene loads each image from `/directory/<filename>`.
