# Global custom React hooks

Shared across features, with no kiosk-specific knowledge.

- `useIdleTimer.ts` — seconds since the last interaction, with a per-second tick.
- `useCarousel.ts` — index carousel with wraparound.
